# Port the server will run on (Render defines this automatically)
PORT=3000

# MongoDB Connection String (Set this to your production database URL on Render)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/project-webdev?retryWrites=true&w=majority
